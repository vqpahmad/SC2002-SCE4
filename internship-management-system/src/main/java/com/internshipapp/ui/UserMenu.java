package com.internshipapp.ui;

//import com.internshipapp.models.User;

/**
 * Interface describing a menu presented to a logged-in user.
 */
public interface UserMenu {
    /**
     * Display the interactive menu for the current user.
     */
    void displayMenu();
    //void displayMenu(Object user);
}